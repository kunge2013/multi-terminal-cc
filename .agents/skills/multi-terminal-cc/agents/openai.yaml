interface:
  display_name: "Multi Terminal Cc"
  short_description: "Repo-specific patterns and workflows for multi-terminal-cc"
  default_prompt: "Use the multi-terminal-cc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true